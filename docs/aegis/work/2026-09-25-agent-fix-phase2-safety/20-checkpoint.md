# Phase 2 Safety and Ecosystem Convergence — Checkpoint

- Active slice: Phase 2D owner convergence and final verification
- Current todo: all code slices are implemented and locally verified, and the hosted matrix is green on the current tree (`ccfaa08`, run 36157984307). The only remaining item is the mechanical `hooks.py` split, now unblocked by that CI evidence
- Completed: result truth/adapter mapping; `agentfix/state.py` persistence owner; atomic writes, ZIP preflight/rollback, bounded source reads and immutable restore snapshots, staged runtime install, directory-swap recovery marker ownership/token/strict-schema validation; MCP lifecycle/batch/protocol; audit completeness and symlink/reparse refusal; Kimi Code and MiniMax Code registry/docs/MCP/env-root contracts; exact MCP ownership checks; Codex TOML ownership cleanup; catalog dead metadata and `ENDPOINTS`/`deadline`/`hooks_install` retirement; backup manifest/layout ownership moved fully into `state.py`; CLI JSON final-boundary masking; mocked and real Windows junction/reparse contracts; split-specific characterization tests; `StatusText`/`StatusLines` explicit adapter status replacing human-text status inference across restore/provider/hooks/MCP/install paths; `mcp/README.md` documents the typed adapter boundary; Windows/POSIX shell-prefix characterization; real Windows junction refusal characterization; macOS Hermes-path and POSIX CLI-shim seam characterization; local owner-invariant regression; CI action/runtime and macOS runner lifecycle updates
- Evidence refs:
  - `python -m unittest discover -s tests -q` — 230 tests, OK on Windows CPython 3.14.6 (17 skips), Windows CPython 3.8.20 (17 skips), and WSL Ubuntu Python 3.12.3 (3 skips)
  - **Hosted CI green on the current tree** — run [36157984307](https://github.com/qingzhuo-cn/agent-fix/actions/runs/36157984307) at `ccfaa08`: ubuntu-22.04 / windows-2022 / macos-15-intel × Python 3.8 / 3.11, all six jobs success. This is the external verification gate that had been open; the macOS leg had never been observed before this run.
  - Three CI defects found by running the matrix, not by local testing:
    - **The CLI surface step asserted the wrong surface.** It required `fix --help` to contain `--agent`, but `--agent` is a per-subcommand option, so the top-level usage never contains it and all six jobs failed. The assertion now targets `fix check --help`; the explicit-target contract is asserted directly (omitting `--agent` is a nonzero usage error), plus a new assertion that a real user status yields a defined exit code (0/1/2) rather than crashing. Verified by extracting the step body out of the workflow and running it on Windows 3.14 and WSL 3.12 before pushing.
    - **Windows device paths could escape the typed error contract.** `os.path.abspath("CON")` returns the device namespace `\\.\CON`, and `Path("CON").resolve()` raises `OSError [WinError 87]` on Python 3.8. A destination built from a bare reserved name therefore reached `path.parent.mkdir` and failed with a raw `FileNotFoundError [WinError 3]`. `_normalized_persistent_path` now refuses any path landing in `\\.\` or `\\?\` after normalization, and `atomic_write_bytes` converts parent-directory creation failure into a typed `StateError` so no raw `OSError` escapes before the first filesystem effect. Local Windows 3.14 did not reproduce this; it required the CI-version 3.8 interpreter and a bare-name path.
    - **An alias-destination test over-specified its message.** It asserted the refusal text contained `component`, which binds the test to whichever guard fires first. Both the reserved-component guard and the device-namespace guard refuse the same destination, so the test now asserts the refusal itself.
  - `python mcp/smoke_test.py` — handshake OK, exactly 12 explicit-target tools registered, removed bulk tools protocol-rejected
  - `python -m compileall agentfix mcp scripts tests` — OK; `git diff --check` — clean
  - Continuation round (masking boundary and typed-error boundary):
    - **Bare `token` forms are now masked** — `token=`, `{"token": ...}`, `token:`, and `export TOKEN=` all escaped `mask_secrets`, while `api_key`/`access_token`/`secret`/query-string/bearer forms were covered. Agent config files routinely use exactly `{"token": ...}`, so any output echoing config content leaked a real credential. `token` is now an alternative in the assignment and JSON/YAML patterns; `key` is deliberately still excluded because it matches too much ordinary text, and `*_key` continues to match via the `api_key` alternative. Verified that ordinary prose is unchanged.
    - **`tempfile.mkstemp` failures no longer escape untyped** — temp creation happens before each guarded block, so an `OSError` (disk full, permission denied) propagated raw out of `atomic_write_bytes`, `create_zip_atomic`, `snapshot_zip`, and `_restore_transaction`. `provider_text` only catches `StateError`, so a provider apply could raise a bare `OSError` at the MCP layer; it now returns a clean `error` status with the key masked. All five call sites convert to `StateError`.
    - Provider write path re-verified (read-only, no real write performed): `apply=true` writes only for the explicitly named `claude-code` target, refuses a symlinked target, preserves unrelated existing `env` keys, writes through `state.atomic_write_json`, and never echoes the supplied key.
  - Round 11 adapter/lifecycle defects (reproduction-verified, each with a regression test):
    - **Uninstall no longer reports indeterminate cleanup as success** — `uninstall_target` only collected `error` statuses, so hook or MCP steps returning `inconclusive` produced `ok` plus a "done" line, and the CLI mapped that to exit 0. A cleanup that cannot confirm a removal is now `error` (nonzero exit); a genuinely clean uninstall still returns `ok`.
    - **Install refuses to clobber an unowned launcher** — the Windows branch of `_install_cli` wrote `~/bin/fix.cmd` with no ownership check, so a foreign same-named script was silently destroyed. Both launcher forms now verify an agent-fix ownership marker before replacing an existing file; agent-fix's own launcher remains replaceable.
    - **Codex hook cleanup no longer deletes lines that merely mention the terms** — removal matched `session_start` + `selfheal` + `agent-fix` as substrings, so a user comment containing all three was removed. It now matches the shape of the entry we install (`selfheal` + `fix.py`).
    - **`_run_cmd` output is masked** — subprocess stdout/stderr reached MCP callers verbatim (the exception path was already masked), leaking API tokens emitted by the wrapped CLI. Output now crosses `rep.mask_secrets` on both the success and failure paths.
    - Considered and rejected: tightening `_codex_marker_owned` to require the block body to embed the runtime path. The existing contract treats the `[mcp_servers.agent-fix]` section name as the ownership marker, and the stricter rule stranded genuinely installed legacy entries (`test_codex_owned_block_with_blanks_and_duplicate_is_removed_cleanly`), which is a worse failure than the narrow case of a user hand-writing into our own namespace. Documented in the function docstring rather than changed.
  - Round 10 adversarial additions (all reproduction-verified before and after, each with a regression test):
    - **ZIP trailing-data bypass closed** — a one-byte tail after a legitimate 10,000-entry archive, combined with a forged EOCD count of 1, previously hit the EOCD bounds check, took the "false positive signature" early return, and skipped the central-directory walk entirely while `zipfile` still parsed the full archive (~5.6 MB peak). That path is now a typed `StateError` (`backup has trailing data after its central directory`); measured peak is ~0.07 MB.
    - **Parent-directory redirect detected** — renaming the parent and substituting an attacker directory during the `os.replace` window used to report success. `atomic_write_bytes` and `create_zip_atomic` now capture the parent identity and re-verify it before and after the commit; on mismatch the rollback is deliberately skipped and the error says so explicitly rather than falsely claiming the target was restored.
    - **Rollback never writes into a replaced parent** — `_restore_transaction` and `_rollback_swapped_directory` capture the parent identity at snapshot/stage time and refuse to deposit the old bytes or the only good backup into a directory that was swapped out. Verified: the attacker directory stays empty and the committed data plus backup remain in the real parent.
    - **Rollback refuses unknown object types** — a target replaced by a plain directory or a junction after the commit is reported and left intact rather than deleted; evidence (marker + backup) is preserved whenever the installed target does not match the staged identity, for reparse points and regular directories alike.
    - **Swap marker proves the backup** — the marker now records the backup identity for `old-moved`/`new-installed` and the pre-rename old-target identity for the `prepared` crash window, so a backup replaced in place is refused instead of installed. Verified: tampered backups are rejected, genuine crash windows still recover.
    - **Snapshot identity is carried to the read** — `snapshot_zip` now returns `(path, identity)` and `read_snapshotted_bytes` verifies it, closing the window where an archive swapped in after snapshot creation was restored while the report claimed success. The prior "tolerate replacement after layout" expectation is now a refusal.
    - **Windows device aliases refused on every write entrypoint** — `CON`, `COM1`, `AUX`, `nul.txt`, and nested forms are rejected by `atomic_write_bytes`/`create_zip_atomic` (which previously bypassed the normalizer entirely) and by directly supplied restore destinations, on Windows only so ordinary POSIX filenames are unaffected.
  - WSL Ubuntu 24.04/ Python 3.12.3 `python3 -m unittest discover -s tests -q` — 219 tests, OK, 1 platform skip (prior 208-test run superseded)
  - Windows CPython 3.8.20, Windows CPython 3.14.6, and WSL Ubuntu Python 3.12.3 `mcp/smoke_test.py` — all 12 explicit-target tools passed; removed bulk tools rejected
  - `python -m compileall agentfix mcp scripts tests` — all Python files compiled OK
  - persistence-owner/status-retirement gate — `agentfix/state.py` remains the only archive/atomic-write owner (`zipfile.ZipFile` and `atomic_write_bytes` appear only there); `result.py` has no marker-based status inference; passed locally
  - `test_persistence_and_adapter_owners_are_single` — local owner invariant regression passed
  - Adversarial replacement regressions — concurrent temp/stage swap between the final identity check and `os.replace` is detected post-commit and rolled back for `atomic_write_bytes`, `create_zip_atomic`, `_restore_transaction`, and `atomic_replace_directory`; rollback restores through a fresh private temp plus `os.replace` and never opens the target for writing, so a hardlink or reparse planted at the target cannot redirect the restore onto an external inode, and a swap of the rollback temp itself (before or after its own commit) is detected and retried up to three times before failing closed; directory-swap crash recovery verifies the stage identity recorded in the marker before adopting a target, rolling back to the backup or failing closed with the marker preserved; that identity is `dev`/`ino` **plus** a hash of the directory's immediate entry names, because deleting a directory frees its inode and a replacement in the same parent can be handed the very same inode number — `dev`/`ino` alone was bypassable and was caught on WSL
  - Archive consistency — restore validates the layout and reads members from one bounded immutable byte buffer, so replacing the snapshot path after layout cannot change restored bytes; aggregate rollback budget, ZIP member metadata, archive comment, and raw archive budgets are enforced
  - ZIP parser budget is enforced before parsing — the end-of-central-directory record is read first, the claimed entry count is cross-checked against the central-directory offset/size, and the directory is walked record-by-record with a fixed 46-byte buffer, so a forged or oversized entry count is refused at ~0.07 MB peak instead of materialising tens of megabytes of `ZipInfo` records; Windows reserved device names also cover superscript digits (`COM³`, `LPT²`) without over-refusing ordinary names such as `COM³-party`
  - Error boundaries — every public persistence API (`read_archive_bytes`, `read_backup_layout`, `inspect_zip`, `snapshot_zip`, `restore_zip_members`, `atomic_write_*`, `create_zip_atomic`) returns a typed `StateError` for unusable input such as an embedded NUL instead of leaking a raw `ValueError`
  - Directory-swap privacy — the crash-window backup and the installed target are both tightened to `0700` immediately after their commit point rather than after marker/backup cleanup, so a crash in between cannot leave a world-readable copy of the old or new config; POSIX permission regressions cover both seams (Windows ACL semantics remain unverified)
  - Cleanup reporting — temp/snapshot/restore cleanup failures are now raised rather than swallowed, including the combined "restore failed **and** cleanup failed" case; refusal branches in `backup_text`/`restore_text` pass through `rep.mask_secrets`; the post-normalization re-check in `backup_text` and walk/stat rechecks in `audit_result` return typed `StatusText`/INCONCLUSIVE results instead of raising
  - CI workflow now uses `actions/checkout@v5`, `actions/setup-python@v6`, supported `macos-15-intel`, a 20-minute job timeout, and step-level `TMPDIR=${{ runner.temp }}` for temp-sensitive tests; lifecycle basis: [Node 20 retirement](https://github.blog/changelog/2026-09-23-node-20-is-no-longer-available-in-github-actions/) and [macOS 13 retirement](https://github.blog/changelog/2025-09-19-github-actions-macos-13-runner-image-is-closing-down/)
  - Prior Windows CPython 3.11.15 evidence covered the pre-final persistence slice; it is not counted as current-tree final proof
  - `go run ... actionlint` — unavailable because the Go proxy could not be reached; no local `actionlint` binary is installed
  - catalog invariant check — 15 agents, 10 issues, unique IDs and valid targets
  - `git diff --check` — clean
  - historical remote CI run [33316118837](https://github.com/qingzhuo-cn/agent-fix/actions/runs/33316118837) passed at baseline HEAD `541281801b998cb11dbef32521f48026348ae286`; it does not cover the dirty current tree
- Baseline: branch `main`; work was intentionally dirty before this phase and is now committed. Sequence: `e4eaf07` (Phase 1-2 body) → `185bed4` (CLI surface CI assertion) → `9ed345b` (Windows device-namespace refusal) → `ccfaa08` (alias test decoupled from guard order), all pushed to `origin/main`
- Uncovered/residual risk: the hosted matrix is now green on `ccfaa08`, so current-tree Ubuntu/Windows/macOS evidence exists; still unverified are Windows ACL behavior, real macOS filesystem semantics beyond the CI leg, and process-level crash windows (rollback/recovery is not crash-atomic); independent `actionlint` validation was unavailable because the Go proxy was unreachable; Windows symlink-target privilege skips remain; residual check-then-rename windows are mitigated by post-commit identity verification plus rollback, but stdlib `os.replace` cannot make the rename itself kernel-atomic against a same-user concurrent writer; on Windows a locked temp file may still leave a plaintext `.restore`/`.tmp` artifact that the caller is told about explicitly
- Blockers: none for the code slices. The user authorized commit/push in this session, which removed the previous "no commit/push authorized" gate
- Incident (round 10, resolved): a review subagent spawned a child that executed `hooks.install_target('opencode')` against the real user profile before its mocks were active, violating the read-only contract. Confirmed impact: `~/.config/opencode/skills/agent-fix/` copied, `~/.config/opencode/AGENTS.md` created, and `~/bin/fix.cmd` created. `~/.codex/*` and `~/.claude/settings.json` were never touched. After explicit user authorization, all four artifacts were removed; their `CreationTime` (21:07:04–21:07:17) proved none pre-dated the incident, so removal restored the exact prior state with no loss. An earlier subagent report claiming the AGENTS.md hook "already existed" was wrong. Lesson: review subagents must be told not to spawn further children, and any delegated audit touching `hooks`/install paths must run with filesystem writes confined to a temporary directory
- Drift: continue; no new compatibility owner, bulk repair, startup hook, or intentional real user-state mutation introduced
- Resume order: the mechanical `hooks.py` split is now unblocked (its gate was current-tree hosted CI green) and is the last open item; keep `hooks.py` as a compatibility facade during the split and retire it explicitly rather than adding a second forwarding owner
- Safety: no real user-profile mutation; no Git staging, reset, clean, branch, or commit

## Module-split decision

- Evidence-backed split completed: `state.py` owns atomic writes, archive inspection/creation, manifest/layout validation, restore commit/rollback, and directory-swap recovery.
- `hooks.py` remains the compatibility facade for legacy cleanup, MCP registration, and installer orchestration: its internal patch seams are covered by integration tests. The macOS CI gate that previously blocked the split is now satisfied by run 36157984307, so the split is unblocked; it is the last open item and requires a new characterization slice before any mechanical move. Splitting must not leave a second forwarding owner: the facade is retired explicitly, not wrapped.
- The CLI integration seam test now asserts the patched adapter was actually called with the explicit agent, preventing a false-green test after future refactors.
- No second registry, persistence writer, or archive owner was introduced. A future split requires a new characterization slice and explicit retirement of the facade, not a line-count refactor.

- Complexity closure: `hooks.py` is just over the 800-line soft-pressure signal and `engine.py` remains a pre-existing large facade; no second owner was added, and the deferred mechanical split is now unblocked by green current-tree CI
- Anti-entropy record: marker-based adapter status inference was retired in favor of explicit status side-channels; string/list compatibility is preserved, CLI patch seams are asserted, and no persistent-state deletion occurred

## Anti-entropy record

- Deletion class: internal code-retirement; `catalog.json` dead metadata removed, not user data
- Old path: `ENDPOINTS`, engine `deadline` plumbing, unused `hooks_install` wrapper, unused `dynamic`/`dynamic_agent` fields, direct engine ZIP/manifest access, and marker-based adapter status inference in `result.py`
- New canonical owner: catalog-driven explicit checks; `agentfix/state.py` for all persistent/archive state; `hooks.dispatch` for the public disabled install response
- Preserved: public issue IDs, 12 MCP tools, launcher paths, CLI JSON contract, legacy hook marker cleanup, `restore(backup="latest")`
- Verification: static owner invariant, full regression, mocked and real Windows reparse/junction contracts, adapter seam assertion, and MCP smoke
